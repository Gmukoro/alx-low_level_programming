#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer.
 *
 * @int: The int in question
 *
 * Return: The abs of the integer
 */

int _abs(int)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
